
  <h1>マイページ</h1>

      <%= @user.name %>
      <%# <%= User.human_attribute_name('icon')%>: %>
      <%# <%= image_tag(@user.icon.variant(resize:'100x100')) if @user.icon.attached?%><br> %>
    </div>
      <%= link_to '一覧' , publications_path ,class:"btn-sm btn-dark"%>
  </div>