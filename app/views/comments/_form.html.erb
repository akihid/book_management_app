<%= render partial: 'errors/error_messages', locals: { object: comment } %>
<%= form_with(model: [post, comment]) do |f| %>

  <%= f.text_field :content, placeholder: 'コメントを入力', class:'comment_field'%>
  <%= f.submit '送信', class:"btn- btn-primary"%>

<% end %>